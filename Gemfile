# Only needed for previewing the site on your own computer.
# GitHub Pages ignores this file and builds the site with its own, equivalent gem set.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Required by `jekyll serve` on Ruby 3+
gem "webrick", "~> 1.8"
